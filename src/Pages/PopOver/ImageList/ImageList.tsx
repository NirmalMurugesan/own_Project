import { style } from "@mui/system";

interface petData {
  locationData: any;
}
// const ImageListwarp = style(){

// };
export default function ImageLists(props: petData) {
  // console.log(props.locationData.state);
  const propsData = props.locationData.state;

  return (
    <>
      {/* <ImageList>

    </ImageList> */}
    </>
  );
}
